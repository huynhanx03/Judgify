meta {
  name: Update
  type: http
  seq: 5
}

put {
  url: {{base_url}}/problems/{{problem_id}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "title": "Two Sum (Updated)",
    "difficulty": "medium",
    "time_limit_ms": 2000,
    "tag_ids": [1, 2]
  }
}
