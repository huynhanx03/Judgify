meta {
  name: Update
  type: http
  seq: 4
}

put {
  url: {{base_url}}/user-stats/{{user_stats_id}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "total_exp": 5000,
    "rating": 1200
  }
}
