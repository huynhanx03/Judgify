meta {
  name: Update
  type: http
  seq: 4
}

put {
  url: {{base_url}}/ranks/{{rank_id}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "name": "Bronze Updated",
    "min_rating": 50
  }
}
