meta {
  name: Create
  type: http
  seq: 3
}

post {
  url: {{base_url}}/user-traits
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "user_id": 1,
    "trait_id": 1
  }
}

script:post-response {
  if (res.body.data && res.body.data.id) {
    bru.setEnvVar("user_trait_id", res.body.data.id);
  }
}
