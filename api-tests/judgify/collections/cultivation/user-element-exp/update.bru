meta {
  name: Update
  type: http
  seq: 4
}

put {
  url: {{base_url}}/user-element-exps/{{user_element_exp_id}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "exp": 1500
  }
}
