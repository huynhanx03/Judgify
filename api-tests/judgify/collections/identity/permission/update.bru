meta {
  name: Update
  type: http
  seq: 4
}

put {
  url: {{base_url}}/permissions/{{permission_id}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "scope": 7
  }
}
