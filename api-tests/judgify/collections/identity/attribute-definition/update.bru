meta {
  name: Update
  type: http
  seq: 4
}

put {
  url: {{base_url}}/attribute-definitions/{{attr_def_id}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "name": "Updated Department"
  }
}
