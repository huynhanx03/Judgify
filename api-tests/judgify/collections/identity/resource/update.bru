meta {
  name: Update
  type: http
  seq: 4
}

put {
  url: {{base_url}}/resources/{{resource_id}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "key": "problem_updated",
    "description": "Updated resource"
  }
}
