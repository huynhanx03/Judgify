meta {
  name: Reset Password
  type: http
  seq: 6
}

post {
  url: {{base_url}}/auth/reset-password
  body: json
  auth: none
}

body:json {
  {
    "token": "reset-token-here",
    "new_password": "NewPassword456!"
  }
}
