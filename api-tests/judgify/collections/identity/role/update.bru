meta {
  name: Update
  type: http
  seq: 4
}

put {
  url: {{base_url}}/roles/{{role_id}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "name": "editor-updated",
    "description": "Updated editor role"
  }
}
